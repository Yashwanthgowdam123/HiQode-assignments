---
#####################################################################
# Install Tomcat and change port
#####################################################################

- name: Install Tomcat
  hosts: "{{ host }}"
  become: yes
  gather_facts: no

  tasks:
    
    - name: Installing updated
      shell: apt update -y && apt install tomcat10 -y
      become: sudo
      become_user: root




